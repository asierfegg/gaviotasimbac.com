# Verificar
Get-ExecutionPolicy

# Si devuelve Restricted o AllSigned, cambiar para esta sesión
Set-ExecutionPolicy Bypass -Scope Process -Force

# Verificar
[Security.Principal.WindowsIdentity]::GetCurrent().Groups -match "S-1-5-32-544"
# Debe devolver True

# Paso 1 — ¿Está registrado?
Get-ItemProperty "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\*" | Where-Object DisplayName -like "*VMware*" | Select-Object DisplayName, PSChildName

# Paso 2 — Parar servicios
@("VMTools","VGAuthService","VM3DService","vmvss") | ForEach-Object { Stop-Service $_ -Force -ErrorAction SilentlyContinue }

# Paso 3 — Forzar desinstalación
msiexec /x "{298083DA-1DC8-4886-97DD-333954302E68}" /qn /l*v C:\vmtools_uninstall.log

# Paso 4 — Verificar resultado (esperar 30 segundos antes)
Get-ItemProperty "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\*" | Where-Object DisplayName -like "*VMware*"
# Si devuelve vacío → OK

